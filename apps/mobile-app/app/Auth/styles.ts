import { Colors } from "@/constants/theme";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	layoutWrapper: {
		flex: 1,
		backgroundColor: Colors.dark.authPageBackground,
		justifyContent: "center",
		alignItems: "center",
	},
});
